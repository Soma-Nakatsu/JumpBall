using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.EventSystems;

public class ClearButton : MonoBehaviour
{
    //// MapSelect変数
    //[SerializeField] private Button mapSelect;
    //// Title変数
    //[SerializeField] private Button title;

    //// 処理したか
    //[SerializeField] public bool onlyOnceFlag;

    //// Clearにあるボタンにフォーカスさせる
    //public void ActiveMenuClear(bool forcusFlag)
    //{
    //    // MapSelectを選択可能にする
    //    mapSelect.interactable = forcusFlag;
    //    // Titleを選択可能にする
    //    title.interactable = forcusFlag;

    //    // ボタンが選択可能なら
    //    if (forcusFlag)
    //    {
    //        // 処理した
    //        if (!onlyOnceFlag)
    //        {
    //            // 最初に選択状態にしたいボタンの設定
    //            mapSelect.Select();

    //            // 処理した
    //            onlyOnceFlag = true;
    //        }
    //    }
    //}
}